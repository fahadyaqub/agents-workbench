# Workflow Redirect: Debugging with SigNoz

The canonical SigNoz workflow now lives at:

```text
shared/workflows/debugging-signoz/debugging-signoz.md
```

Load that file for SigNoz log investigations. This redirect remains so older
references to `shared/workflows/debugging-signoz.md` continue to work.
